export const config = {
  // When false, users cannot select express/fast (priority) order.
  enablePriorityOrder: true,
  // When false, premium packaging is hidden/disabled in v2 flow.
  enablePremiumPackaging: false
};



